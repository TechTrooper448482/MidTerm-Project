import 'package:flutter/material.dart';

class DetailsStyle extends StatelessWidget {
  const DetailsStyle({Key?key,});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}